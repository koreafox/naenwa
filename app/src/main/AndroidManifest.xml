<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 네트워크 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 플로팅 버튼 (오버레이) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 화면 캡처 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- 음성 인식 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- 포그라운드 서비스 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- APK 설치 권한 (원격 빌드 후 설치용) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- 카메라 (QR 스캔) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".NaenwaApp"
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:icon="@android:drawable/ic_dialog_info"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.NaenwaRemote"
        tools:targetApi="28">

        <!-- Main Activity (Launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.NaenwaRemote">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- QR Scanner Activity (서버 URL 스캔용) -->
        <activity
            android:name=".QrScannerActivity"
            android:exported="false"
            android:theme="@style/Theme.NaenwaRemote" />

        <!-- GitHub Login Activity -->
        <activity
            android:name=".GitHubLoginActivity"
            android:exported="true"
            android:theme="@style/Theme.NaenwaRemote">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="naenwa"
                    android:host="callback" />
            </intent-filter>
        </activity>

        <!-- Project Select Activity -->
        <activity
            android:name=".ProjectSelectActivity"
            android:exported="false"
            android:theme="@style/Theme.NaenwaRemote" />

        <!-- WebContainer Test Activity -->
        <activity
            android:name=".WebContainerTestActivity"
            android:exported="false"
            android:theme="@style/Theme.NaenwaRemote" />

        <!-- Local Mode Activity (WebView IDE) -->
        <activity
            android:name=".LocalModeActivity"
            android:exported="false"
            android:theme="@style/Theme.NaenwaRemote"
            android:windowSoftInputMode="adjustResize" />

        <!-- Terminal Activity -->
        <activity
            android:name=".terminal.TerminalActivity"
            android:exported="false"
            android:theme="@style/Theme.NaenwaRemote"
            android:windowSoftInputMode="adjustResize" />

        <!-- Claude OAuth Login Activity -->
        <activity
            android:name=".claude.ClaudeLoginActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.NaenwaRemote">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="claude-code"
                    android:host="oauth"
                    android:pathPrefix="/callback" />
            </intent-filter>
        </activity>

        <!-- 플로팅 버튼 서비스 (백그라운드 연결 유지) -->
        <service
            android:name=".service.FloatingService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|dataSync" />

        <!-- FileProvider for APK installation (Android 7+) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
